{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Candidatos list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Surnames</th>
                <th>Name</th>
                <th>Email</th>
                <th>Documentnumber</th>
                <th>Department</th>
                <th>Votes</th>
                <th>Registerdate</th>
                <th>Updatedate</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('candidatos_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.surnames }}</td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.email }}</td>
                <td>{{ entity.documentNumber }}</td>
                <td>{{ entity.department }}</td>
                <td>{{ entity.votes }}</td>
                <td>{% if entity.registerDate %}{{ entity.registerDate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.updateDate %}{{ entity.updateDate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('candidatos_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('candidatos_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('candidatos_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
